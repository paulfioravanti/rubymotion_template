# This is just a test UIView to remove or modify to be more Ruby-like
class ExampleLabel < UILabel
  include UIViewHelper

  # Obj-C-style properties left here on the assumption they'll be replaced.
  # Create new snake_case methods that call these in a helper, or use another
  # Rubymotion gem like Teacup for view item styling
  def initialize
    self.frame = DeviceScreen.main_screen.bounds
    self.text = I18n.t('welcome', name: '<%= name %>')
    self.backgroundColor = UIColor.whiteColor
    self.font = UIFont.boldSystemFontOfSize(30)
    self.textColor = UIColor.blackColor
    self.textAlignment = UITextAlignmentCenter
    self.numberOfLines = 0
  end
end