describe "Application '<%= name %>'" do
  extend SpecHelper

  before { @app = Application.application_instance }

  it "has one window" do
    @app.windows.size.should.equal(1)
  end

  describe "root view controller" do
    before { @root_controller = @app.key_window.root_view_controller }

    it "is an example" do
       @root_controller.class.should.equal(ExampleController)
    end
  end
end
